{
  "name": "islamic-google-modern",
  "version": "1.0.0",
  "description": "Modern Islamic Google-style site (search + Ask AI). Set env vars before deploying to Vercel.",
  "main": "index.js",
  "scripts": {
    "start": "vercel dev"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "@google/generative-ai": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}